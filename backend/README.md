Backend service for CivicConnect.

Commands
```
cd backend
npm install
npm run dev
```

API base: `/api/v1`

Next steps:
- Implement Supabase Auth middleware
- Add controllers for tickets, permits, announcements
- Add AI worker integration for Gemini
